/*import { CommerceEntity } from "../../domain/commerce.entity";
import { CommerceRepository } from "../../domain/commerce.repository";

const mock_score = {
  id: 'uidMock',
  name: 'string',
  email: 'string',
  countryCode: 'string',
  city: 'string',
  totalFreePrevent: 1,
  isActive: true,
  dataFinish: 'dasd'
};

export class MockRepository implements CommerceRepository {
  async createCommerce(commerce: CommerceEntity): Promise<CommerceEntity> {
    console.log('estoy en mock repository');
    console.log(commerce);
    const res = commerce;
    return res;
  }
  async deleteCommerce(uid: string): Promise<Boolean> {
    return true;
  }
}*/
